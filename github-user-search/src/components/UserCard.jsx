// This is a placeholder file to ensure the 'components' directory is not empty.
import React from 'react';

const UserCard = () => {
    return (
        <div>
            {/* User card content will go here */}
        </div>
    );
};

export default UserCard;