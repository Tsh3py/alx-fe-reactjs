import React from 'react';

// Initialize a Context and export it
const UserContext = React.createContext(null); // You can provide a default value here, e.g., an empty object

export default UserContext;
